import { DealerFilterPipePipe } from './dealer-filter-pipe.pipe';

describe('DealerFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DealerFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
